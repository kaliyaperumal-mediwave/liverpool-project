apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: service-diary
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: service-diary
    spec:
      imagePullSecrets:
      - name: acr-credentials
      containers:
        - name: service-diary
          image: mindwavephr.azurecr.io/phr-service-diary:v755
          env:
          - name: APP_PORT
            value: '3009'
          - name: DATABASE_URL
            value: https://service-database:3001
          - name: NODE_ENV
            value: development
          - name: NODE_TLS_REJECT_UNAUTHORIZED
            value: '0'
          ports:
            - containerPort: 3009
      imagePullSecrets:
       - name: regcred            

---
apiVersion: v1
kind: Service
metadata:
  name: service-diary
  labels:
    name: service-diary
spec:
  ports:
    - port: 3009
      targetPort: 3009
      protocol: TCP
  selector:
    app: service-diary
  type: ClusterIP
