apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: service-mood
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: service-mood
    spec:
      imagePullSecrets:
      - name: acr-credentials
      containers:
        - name: service-mood
          image: mindwavephr.azurecr.io/phr-service-mood:v755
          env:
          - name: APP_PORT
            value: '3022'
          - name: DATABASE_URL
            value: https://service-database:3001
          - name: NODE_ENV
            value: development
          - name: NODE_TLS_REJECT_UNAUTHORIZED
            value: '0'
          ports:
            - containerPort: 3022
      imagePullSecrets:
       - name: regcred            

---
apiVersion: v1
kind: Service
metadata:
  name: service-mood
  labels:
    name: service-mood
spec:
  ports:
    - port: 3022
      targetPort: 3022
      protocol: TCP
  selector:
    app: service-mood
  type: ClusterIP
