apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: tls-secret
spec:
  secretName: tls-secret
  dnsNames:
  - <DNS>
  acme:
    config:
    - http01:
        ingressClass: nginx
      domains:
      - <Domain>
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
