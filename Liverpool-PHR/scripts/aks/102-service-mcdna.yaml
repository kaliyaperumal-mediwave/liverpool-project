apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: service-mcdna
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: service-mcdna
    spec:
      imagePullSecrets:
      - name: acr-credentials
      containers:
        - name: service-mcdna
          image: mindwavephr.azurecr.io/phr-service-mcdna:v755
          env:
          - name: NODE_ENV
            value: development
          - name: APP_NAME
            value: service-mcdna
          - name: APP_PORT
            value: '4002'
          - name: MSSQL_DB
            value: 'phr-staging-epjs'
          - name: MSSQL_USER
            value: 'staginguser'
          - name: MSSQL_PASSWORD
            value: 'JWffVAlV8o1eM7ig'
          - name: MSSQL_HOST
            value: 'phr-staging-epjs.database.windows.net'
          - name: MSSQL_PORT
            value: '1433'
          - name: DATABASE_URL
            value: https://service-database:3001
          - name: NODE_TLS_REJECT_UNAUTHORIZED
            value: '0'
          ports:
            - containerPort: 4002
      imagePullSecrets:
       - name: regcred            

---
apiVersion: v1
kind: Service
metadata:
  name: service-mcdna
  labels:
    name: service-mcdna
spec:
  ports:
    - port: 4002
      targetPort: 4002
      protocol: TCP
  selector:
    app: service-mcdna
  type: ClusterIP
