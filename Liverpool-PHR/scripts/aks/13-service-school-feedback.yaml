apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: service-school-feedback
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: service-school-feedback
    spec:
      imagePullSecrets:
      - name: acr-credentials
      containers:
        - name: service-school-feedback
          image: mindwavephr.azurecr.io/phr-service-school-feedback:v755
          env:
          - name: APP_PORT
            value: '3013'
          - name: DATABASE_URL
            value: https://service-database:3001
          - name: NODE_ENV
            value: development
          - name: NODE_TLS_REJECT_UNAUTHORIZED
            value: '0'
          ports:
            - containerPort: 3013
      imagePullSecrets:
       - name: regcred            

---
apiVersion: v1
kind: Service
metadata:
  name: service-school-feedback
  labels:
    name: service-school-feedback
spec:
  ports:
    - port: 3013
      targetPort: 3013
      protocol: TCP
  selector:
    app: service-school-feedback
  type: ClusterIP
