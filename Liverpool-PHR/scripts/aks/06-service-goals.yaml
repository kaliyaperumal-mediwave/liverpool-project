apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: service-goals
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: service-goals
    spec:
      imagePullSecrets:
      - name: acr-credentials
      containers:
        - name: service-goals
          image: mindwavephr.azurecr.io/phr-service-goals:v755
          env:
          - name: APP_PORT
            value: '3004'
          - name: DATABASE_URL
            value: https://service-database:3001
          - name: NODE_ENV
            value: development
          - name: NODE_TLS_REJECT_UNAUTHORIZED
            value: '0'
          ports:
            - containerPort: 3004
      imagePullSecrets:
       - name: regcred            

---
apiVersion: v1
kind: Service
metadata:
  name: service-goals
  labels:
    name: service-goals
spec:
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
  selector:
    app: service-goals
  type: ClusterIP
